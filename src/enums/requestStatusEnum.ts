export enum RequestStatusEnum {
  pending = 'pending',
  quoted = 'quoted',
  accepted = 'accepted',
  in_progress = 'in_progress',
  completed = 'completed',
  paid = 'paid',
  cancelled = 'cancelled',
  disputed = 'disputed',
}
